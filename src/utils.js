//Utility functions or variables

const sum = (op1, op2) => {
    return op1 + op2;
}

const sub = (op1, op2) => {
    return op1 - op2;
}

const mul= (op1, op2) => {
    return op1 * op2;
}

export default sum;
export{ mul, sub};

